{
  "name": "rag-demo",
  "private": true,
  "scripts": {
    "aic": "aic -t conventional",
    "format": "biome format --write .",
    "lint": "biome lint .",
    "lint:fix": "biome lint --write --unsafe .",
    "check": "biome check .",
    "check:fix": "biome check --write .",
    "db:migrate": "npm run migrate --prefix apps/api",
    "db:generate": "npm run generate --prefix apps/api"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.13"
  }
}
